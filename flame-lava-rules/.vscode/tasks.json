{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "write-test",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "echo ok>tmp_task_write.txt"
            ]
        },
        {
            "label": "patch-run-bundle-imports",
            "type": "shell",
            "command": "$p='scripts/run_parse_bundle.py'; $t=Get-Content -Raw -Path $p; $t=$t.Replace('from datetime import datetime','from datetime import datetime, timezone'); $t=$t.Replace('from typing import List','from typing import Dict, List, Optional'); Set-Content -Path $p -Value $t -Encoding utf8"
        },
        {
            "label": "patch-run-bundle-add-metadata-fns",
            "type": "shell",
            "command": "$p='scripts/run_parse_bundle.py'; $t=Get-Content -Raw -Path $p; $needle=\"def parse_args() -> argparse.Namespace:\"; $insert=@'\n\ndef resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:\n    if not raw:\n        return None\n    p = Path(raw)\n    return str(p.resolve()) if p.exists() else None\n\n\ndef write_run_metadata(\n    run_dir: Path,\n    args: argparse.Namespace,\n    generated: Dict[str, str],\n    repo_resolved: Optional[Path],\n    qdf_dir_used: Optional[str],\n    lineitem_report_used: Optional[str],\n) -> Path:\n    source_path = Path(args.source)\n    source_resolved = str(source_path.resolve()) if source_path.exists() else None\n\n    flame_repos: List[str] = []\n    if repo_resolved is not None:\n        flame_repos.append(str(repo_resolved))\n\n    payload = {\n        \"created_utc\": datetime.now(timezone.utc).isoformat(),\n        \"run_dir\": str(run_dir),\n        \"workflow_type\": args.type,\n        \"compat_mode\": args.compat_mode,\n        \"ffr_source\": {\n            \"input\": args.source,\n            \"resolved\": source_resolved,\n        },\n        \"flame_git_repos_parsed\": flame_repos,\n        \"inputs\": {\n            \"repo_input\": args.repo,\n            \"repo_resolved\": str(repo_resolved) if repo_resolved else None,\n            \"lineitem_report\": lineitem_report_used,\n            \"lineitem_report_resolved\": resolved_path_if_exists(lineitem_report_used),\n            \"qdf_dir\": qdf_dir_used,\n            \"qdf_dir_resolved\": resolved_path_if_exists(qdf_dir_used),\n        },\n        \"files\": generated,\n    }\n\n    metadata_path = run_dir / \"run_metadata.json\"\n    metadata_path.write_text(json.dumps(payload, indent=2), encoding=\"utf-8\")\n    return metadata_path\n\n'@; if ($t.Contains($needle) -and -not $t.Contains('def write_run_metadata(')) { $t = $t.Replace($needle, $insert + $needle) }; Set-Content -Path $p -Value $t -Encoding utf8"
        },
        {
            "label": "patch-run-bundle-add-metadata-fns-2",
            "type": "shell",
            "command": "$p='scripts/run_parse_bundle.py'; $t=Get-Content -Raw -Path $p; $needle='def parse_args() -> argparse.Namespace:'; $insert=@'\n\ndef resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:\n    if not raw:\n        return None\n    p = Path(raw)\n    return str(p.resolve()) if p.exists() else None\n\n\ndef write_run_metadata(\n    run_dir: Path,\n    args: argparse.Namespace,\n    generated: Dict[str, str],\n    repo_resolved: Optional[Path],\n    qdf_dir_used: Optional[str],\n    lineitem_report_used: Optional[str],\n) -> Path:\n    source_path = Path(args.source)\n    source_resolved = str(source_path.resolve()) if source_path.exists() else None\n\n    flame_repos: List[str] = []\n    if repo_resolved is not None:\n        flame_repos.append(str(repo_resolved))\n\n    payload = {\n        \"created_utc\": datetime.now(timezone.utc).isoformat(),\n        \"run_dir\": str(run_dir),\n        \"workflow_type\": args.type,\n        \"compat_mode\": args.compat_mode,\n        \"ffr_source\": {\n            \"input\": args.source,\n            \"resolved\": source_resolved,\n        },\n        \"flame_git_repos_parsed\": flame_repos,\n        \"inputs\": {\n            \"repo_input\": args.repo,\n            \"repo_resolved\": str(repo_resolved) if repo_resolved else None,\n            \"lineitem_report\": lineitem_report_used,\n            \"lineitem_report_resolved\": resolved_path_if_exists(lineitem_report_used),\n            \"qdf_dir\": qdf_dir_used,\n            \"qdf_dir_resolved\": resolved_path_if_exists(qdf_dir_used),\n        },\n        \"files\": generated,\n    }\n\n    metadata_path = run_dir / \"run_metadata.json\"\n    metadata_path.write_text(json.dumps(payload, indent=2), encoding=\"utf-8\")\n    return metadata_path\n\n'@; if ($t.Contains($needle) -and -not $t.Contains('def write_run_metadata(')) { $t = $t.Replace($needle, $insert + $needle) }; Set-Content -Path $p -Value $t -Encoding utf8"
        },
        {
            "label": "patch-run-bundle-main-metadata",
            "type": "shell",
            "command": "$p='scripts/run_parse_bundle.py'; $t=Get-Content -Raw -Path $p; $t=$t.Replace('    generated = {}','    generated: Dict[str, str] = {}'); $t=$t.Replace('    lineitem_csv = run_dir / \"LineItemAttributes_Report.csv\"`r`n    lineitem_json = run_dir / \"lineitem_attributes.json\"`r`n','    lineitem_csv = run_dir / \"LineItemAttributes_Report.csv\"`r`n    lineitem_json = run_dir / \"lineitem_attributes.json\"`r`n`r`n    repo_resolved: Optional[Path] = None`r`n    qdf_dir_used: Optional[str] = None`r`n    lineitem_report_used: Optional[str] = None`r`n'); $t=$t.Replace('        repo = Path(args.repo or args.source).resolve()','        repo_resolved = Path(args.repo or args.source).resolve()'); $t=$t.Replace('            str(repo),','            str(repo_resolved),'); $t=$t.Replace('        if not qdf_dir:`r`n            raise SystemExit(\"--qdf-dir is required for qdf mode (or use --compat-mode ffr)\")`r`n`r`n        qdf_csv = run_dir / \"qdf_validation_from_ffr.csv\"','        if not qdf_dir:`r`n            raise SystemExit(\"--qdf-dir is required for qdf mode (or use --compat-mode ffr)\")`r`n`r`n        lineitem_report_used = str(lineitem_report)`r`n        qdf_dir_used = str(qdf_dir)`r`n`r`n        qdf_csv = run_dir / \"qdf_validation_from_ffr.csv\"'); $t=$t.Replace('    print(json.dumps({\"run_dir\": str(run_dir), \"files\": generated}, indent=2))','    metadata_path = write_run_metadata(`r`n        run_dir=run_dir,`r`n        args=args,`r`n        generated=generated,`r`n        repo_resolved=repo_resolved,`r`n        qdf_dir_used=qdf_dir_used,`r`n        lineitem_report_used=lineitem_report_used,`r`n    )`r`n    generated[\"run_metadata_json\"] = str(metadata_path)`r`n`r`n    print(json.dumps({\"run_dir\": str(run_dir), \"files\": generated}, indent=2))'); Set-Content -Path $p -Value $t -Encoding utf8"
        },
        {
            "label": "compile-run-bundle",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "rewrite-run-parse-bundle",
            "type": "shell",
            "command": "$content = @'\n#!/usr/bin/env python3\n\"\"\"Managed parse runner for Flame/FFR workflows.\n\nCreates one output folder per source, and appends a timestamp if the same\nsource folder already exists.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport re\nimport subprocess\nimport sys\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Dict, List, Optional\n\n\ndef slugify(value: str) -> str:\n    cleaned = re.sub(r\"[^A-Za-z0-9._-]+\", \"_\", value.strip())\n    cleaned = re.sub(r\"_+\", \"_\", cleaned).strip(\"._-\")\n    return cleaned or \"source\"\n\n\ndef create_run_dir(output_root: Path, source_label: str) -> Path:\n    output_root.mkdir(parents=True, exist_ok=True)\n    base = slugify(source_label)\n    target = output_root / base\n    if target.exists():\n        stamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        target = output_root / f\"{base}_{stamp}\"\n    target.mkdir(parents=True, exist_ok=False)\n    return target\n\n\ndef run_cmd(cmd: List[str]) -> None:\n    proc = subprocess.run(cmd, text=True)\n    if proc.returncode != 0:\n        raise SystemExit(proc.returncode)\n\n\ndef resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:\n    if not raw:\n        return None\n    p = Path(raw)\n    return str(p.resolve()) if p.exists() else None\n\n\ndef write_run_metadata(\n    run_dir: Path,\n    args: argparse.Namespace,\n    generated: Dict[str, str],\n    repo_resolved: Optional[Path],\n    qdf_dir_used: Optional[str],\n    lineitem_report_used: Optional[str],\n) -> Path:\n    source_path = Path(args.source)\n    source_resolved = str(source_path.resolve()) if source_path.exists() else None\n\n    flame_repos: List[str] = []\n    if repo_resolved is not None:\n        flame_repos.append(str(repo_resolved))\n\n    payload = {\n        \"created_utc\": datetime.now(timezone.utc).isoformat(),\n        \"run_dir\": str(run_dir),\n        \"workflow_type\": args.type,\n        \"compat_mode\": args.compat_mode,\n        \"ffr_source\": {\n            \"input\": args.source,\n            \"resolved\": source_resolved,\n        },\n        \"flame_git_repos_parsed\": flame_repos,\n        \"inputs\": {\n            \"repo_input\": args.repo,\n            \"repo_resolved\": str(repo_resolved) if repo_resolved else None,\n            \"lineitem_report\": lineitem_report_used,\n            \"lineitem_report_resolved\": resolved_path_if_exists(lineitem_report_used),\n            \"qdf_dir\": qdf_dir_used,\n            \"qdf_dir_resolved\": resolved_path_if_exists(qdf_dir_used),\n        },\n        \"files\": generated,\n    }\n\n    metadata_path = run_dir / \"run_metadata.json\"\n    metadata_path.write_text(json.dumps(payload, indent=2), encoding=\"utf-8\")\n    return metadata_path\n\n\ndef parse_args() -> argparse.Namespace:\n    parser = argparse.ArgumentParser(description=\"Run lineitem/qdf parsers with managed output folders\")\n    parser.add_argument(\"--source\", required=True, help=\"Source identifier (FFR path or repo path/URL)\")\n    parser.add_argument(\"--type\", choices=[\"lineitem\", \"qdf\", \"both\"], default=\"both\")\n    parser.add_argument(\"--repo\", help=\"Repo path for lineitem parser (defaults to --source)\")\n    parser.add_argument(\"--lineitem-report\", help=\"LineItem CSV for qdf mode (required for qdf-only unless compat ffr default exists)\")\n    parser.add_argument(\"--qdf-dir\", help=\"QDF JSON directory for qdf mode\")\n    parser.add_argument(\"--compat-mode\", choices=[\"ffr\"], help=\"Optional compat mode for defaults\")\n    parser.add_argument(\"--output-root\", default=\"out/runs\", help=\"Root folder where managed run dirs are created\")\n    parser.add_argument(\"--label\", help=\"Optional custom folder label\")\n    return parser.parse_args()\n\n\ndef main() -> int:\n    args = parse_args()\n    source_label = args.label or Path(args.source).name or args.source\n    run_dir = create_run_dir(Path(args.output_root).resolve(), source_label)\n\n    python_exe = sys.executable\n    generated: Dict[str, str] = {}\n\n    lineitem_csv = run_dir / \"LineItemAttributes_Report.csv\"\n    lineitem_json = run_dir / \"lineitem_attributes.json\"\n\n    repo_resolved: Optional[Path] = None\n    qdf_dir_used: Optional[str] = None\n    lineitem_report_used: Optional[str] = None\n\n    if args.type in {\"lineitem\", \"both\"}:\n        repo_resolved = Path(args.repo or args.source).resolve()\n        cmd = [\n            python_exe,\n            \"scripts/lineitem_attribute_parser.py\",\n            \"--repo\",\n            str(repo_resolved),\n            \"--output-csv\",\n            str(lineitem_csv),\n            \"--output-json\",\n            str(lineitem_json),\n        ]\n        run_cmd(cmd)\n        generated[\"lineitem_csv\"] = str(lineitem_csv)\n        generated[\"lineitem_json\"] = str(lineitem_json)\n\n    if args.type in {\"qdf\", \"both\"}:\n        lineitem_report = args.lineitem_report\n        if not lineitem_report and args.type == \"both\":\n            lineitem_report = str(lineitem_csv)\n        if not lineitem_report and args.compat_mode == \"ffr\":\n            lineitem_report = \"out/ffr/lineitem_attributes_from_ffr.csv\"\n        if not lineitem_report:\n            raise SystemExit(\"--lineitem-report is required for qdf mode (or use --type both)\")\n\n        qdf_dir = args.qdf_dir or (\"out/ffr/qdf-json\" if args.compat_mode == \"ffr\" else None)\n        if not qdf_dir:\n            raise SystemExit(\"--qdf-dir is required for qdf mode (or use --compat-mode ffr)\")\n\n        lineitem_report_used = str(lineitem_report)\n        qdf_dir_used = str(qdf_dir)\n\n        qdf_csv = run_dir / \"qdf_validation_from_ffr.csv\"\n        qdf_json = run_dir / \"qdf_validation_from_ffr.json\"\n        heatmap_csv = run_dir / \"qdf_validation_heatmap.csv\"\n        heatmap_json = run_dir / \"qdf_validation_heatmap.json\"\n        heatmap_xlsx = run_dir / \"QDF_LIRA_Validation_Heatmap.xlsx\"\n\n        cmd = [\n            python_exe,\n            \"scripts/qdf_validation_parser.py\",\n            \"--lineitem-report\",\n            str(lineitem_report),\n            \"--qdf-dir\",\n            str(qdf_dir),\n            \"--output-csv\",\n            str(qdf_csv),\n            \"--output-json\",\n            str(qdf_json),\n            \"--output-heatmap-csv\",\n            str(heatmap_csv),\n            \"--output-heatmap-json\",\n            str(heatmap_json),\n            \"--output-heatmap-xlsx\",\n            str(heatmap_xlsx),\n        ]\n        run_cmd(cmd)\n        generated.update(\n            {\n                \"qdf_csv\": str(qdf_csv),\n                \"qdf_json\": str(qdf_json),\n                \"heatmap_csv\": str(heatmap_csv),\n                \"heatmap_json\": str(heatmap_json),\n                \"heatmap_xlsx\": str(heatmap_xlsx),\n            }\n        )\n\n    metadata_path = write_run_metadata(\n        run_dir=run_dir,\n        args=args,\n        generated=generated,\n        repo_resolved=repo_resolved,\n        qdf_dir_used=qdf_dir_used,\n        lineitem_report_used=lineitem_report_used,\n    )\n    generated[\"run_metadata_json\"] = str(metadata_path)\n\n    print(json.dumps({\"run_dir\": str(run_dir), \"files\": generated}, indent=2))\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n'@; Set-Content -Path 'scripts/run_parse_bundle.py' -Value $content -Encoding utf8"
        },
        {
            "label": "compile-run-bundle-2",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "rewrite-run-bundle-via-python",
            "type": "shell",
            "command": "python",
            "args": [
                "-c",
                "from pathlib import Path\np=Path('scripts/run_parse_bundle.py')\np.write_text('''#!/usr/bin/env python3\n\"\"\"Managed parse runner for Flame/FFR workflows.\n\nCreates one output folder per source, and appends a timestamp if the same\nsource folder already exists.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport re\nimport subprocess\nimport sys\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Dict, List, Optional\n\n\ndef slugify(value: str) -> str:\n    cleaned = re.sub(r\"[^A-Za-z0-9._-]+\", \"_\", value.strip())\n    cleaned = re.sub(r\"_+\", \"_\", cleaned).strip(\"._-\")\n    return cleaned or \"source\"\n\n\ndef create_run_dir(output_root: Path, source_label: str) -> Path:\n    output_root.mkdir(parents=True, exist_ok=True)\n    base = slugify(source_label)\n    target = output_root / base\n    if target.exists():\n        stamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        target = output_root / f\"{base}_{stamp}\"\n    target.mkdir(parents=True, exist_ok=False)\n    return target\n\n\ndef run_cmd(cmd: List[str]) -> None:\n    proc = subprocess.run(cmd, text=True)\n    if proc.returncode != 0:\n        raise SystemExit(proc.returncode)\n\n\ndef resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:\n    if not raw:\n        return None\n    path = Path(raw)\n    return str(path.resolve()) if path.exists() else None\n\n\ndef write_run_metadata(\n    run_dir: Path,\n    args: argparse.Namespace,\n    generated: Dict[str, str],\n    repo_resolved: Optional[Path],\n    qdf_dir_used: Optional[str],\n    lineitem_report_used: Optional[str],\n) -> Path:\n    source_path = Path(args.source)\n    source_resolved = str(source_path.resolve()) if source_path.exists() else None\n\n    flame_repos: List[str] = []\n    if repo_resolved is not None:\n        flame_repos.append(str(repo_resolved))\n\n    payload = {\n        \"created_utc\": datetime.now(timezone.utc).isoformat(),\n        \"run_dir\": str(run_dir),\n        \"workflow_type\": args.type,\n        \"compat_mode\": args.compat_mode,\n        \"ffr_source\": {\n            \"input\": args.source,\n            \"resolved\": source_resolved,\n        },\n        \"flame_git_repos_parsed\": flame_repos,\n        \"inputs\": {\n            \"repo_input\": args.repo,\n            \"repo_resolved\": str(repo_resolved) if repo_resolved else None,\n            \"lineitem_report\": lineitem_report_used,\n            \"lineitem_report_resolved\": resolved_path_if_exists(lineitem_report_used),\n            \"qdf_dir\": qdf_dir_used,\n            \"qdf_dir_resolved\": resolved_path_if_exists(qdf_dir_used),\n        },\n        \"files\": generated,\n    }\n\n    metadata_path = run_dir / \"run_metadata.json\"\n    metadata_path.write_text(json.dumps(payload, indent=2), encoding=\"utf-8\")\n    return metadata_path\n\n\ndef parse_args() -> argparse.Namespace:\n    parser = argparse.ArgumentParser(description=\"Run lineitem/qdf parsers with managed output folders\")\n    parser.add_argument(\"--source\", required=True, help=\"Source identifier (FFR path or repo path/URL)\")\n    parser.add_argument(\"--type\", choices=[\"lineitem\", \"qdf\", \"both\"], default=\"both\")\n    parser.add_argument(\"--repo\", help=\"Repo path for lineitem parser (defaults to --source)\")\n    parser.add_argument(\"--lineitem-report\", help=\"LineItem CSV for qdf mode (required for qdf-only unless compat ffr default exists)\")\n    parser.add_argument(\"--qdf-dir\", help=\"QDF JSON directory for qdf mode\")\n    parser.add_argument(\"--compat-mode\", choices=[\"ffr\"], help=\"Optional compat mode for defaults\")\n    parser.add_argument(\"--output-root\", default=\"out/runs\", help=\"Root folder where managed run dirs are created\")\n    parser.add_argument(\"--label\", help=\"Optional custom folder label\")\n    return parser.parse_args()\n\n\ndef main() -> int:\n    args = parse_args()\n    source_label = args.label or Path(args.source).name or args.source\n    run_dir = create_run_dir(Path(args.output_root).resolve(), source_label)\n\n    python_exe = sys.executable\n    generated: Dict[str, str] = {}\n\n    lineitem_csv = run_dir / \"LineItemAttributes_Report.csv\"\n    lineitem_json = run_dir / \"lineitem_attributes.json\"\n\n    repo_resolved: Optional[Path] = None\n    qdf_dir_used: Optional[str] = None\n    lineitem_report_used: Optional[str] = None\n\n    if args.type in {\"lineitem\", \"both\"}:\n        repo_resolved = Path(args.repo or args.source).resolve()\n        cmd = [\n            python_exe,\n            \"scripts/lineitem_attribute_parser.py\",\n            \"--repo\",\n            str(repo_resolved),\n            \"--output-csv\",\n            str(lineitem_csv),\n            \"--output-json\",\n            str(lineitem_json),\n        ]\n        run_cmd(cmd)\n        generated[\"lineitem_csv\"] = str(lineitem_csv)\n        generated[\"lineitem_json\"] = str(lineitem_json)\n\n    if args.type in {\"qdf\", \"both\"}:\n        lineitem_report = args.lineitem_report\n        if not lineitem_report and args.type == \"both\":\n            lineitem_report = str(lineitem_csv)\n        if not lineitem_report and args.compat_mode == \"ffr\":\n            lineitem_report = \"out/ffr/lineitem_attributes_from_ffr.csv\"\n        if not lineitem_report:\n            raise SystemExit(\"--lineitem-report is required for qdf mode (or use --type both)\")\n\n        qdf_dir = args.qdf_dir or (\"out/ffr/qdf-json\" if args.compat_mode == \"ffr\" else None)\n        if not qdf_dir:\n            raise SystemExit(\"--qdf-dir is required for qdf mode (or use --compat-mode ffr)\")\n\n        lineitem_report_used = str(lineitem_report)\n        qdf_dir_used = str(qdf_dir)\n\n        qdf_csv = run_dir / \"qdf_validation_from_ffr.csv\"\n        qdf_json = run_dir / \"qdf_validation_from_ffr.json\"\n        heatmap_csv = run_dir / \"qdf_validation_heatmap.csv\"\n        heatmap_json = run_dir / \"qdf_validation_heatmap.json\"\n        heatmap_xlsx = run_dir / \"QDF_LIRA_Validation_Heatmap.xlsx\"\n\n        cmd = [\n            python_exe,\n            \"scripts/qdf_validation_parser.py\",\n            \"--lineitem-report\",\n            str(lineitem_report),\n            \"--qdf-dir\",\n            str(qdf_dir),\n            \"--output-csv\",\n            str(qdf_csv),\n            \"--output-json\",\n            str(qdf_json),\n            \"--output-heatmap-csv\",\n            str(heatmap_csv),\n            \"--output-heatmap-json\",\n            str(heatmap_json),\n            \"--output-heatmap-xlsx\",\n            str(heatmap_xlsx),\n        ]\n        run_cmd(cmd)\n        generated.update(\n            {\n                \"qdf_csv\": str(qdf_csv),\n                \"qdf_json\": str(qdf_json),\n                \"heatmap_csv\": str(heatmap_csv),\n                \"heatmap_json\": str(heatmap_json),\n                \"heatmap_xlsx\": str(heatmap_xlsx),\n            }\n        )\n\n    metadata_path = write_run_metadata(\n        run_dir=run_dir,\n        args=args,\n        generated=generated,\n        repo_resolved=repo_resolved,\n        qdf_dir_used=qdf_dir_used,\n        lineitem_report_used=lineitem_report_used,\n    )\n    generated[\"run_metadata_json\"] = str(metadata_path)\n\n    print(json.dumps({\"run_dir\": str(run_dir), \"files\": generated}, indent=2))\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n''', encoding='utf-8')"
            ]
        },
        {
            "label": "compile-run-bundle-3",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "py-arg-write-test",
            "type": "shell",
            "command": "python",
            "args": [
                "-c",
                "from pathlib import Path; Path('tmp_py_arg_test.txt').write_text('\"abc\"\\n', encoding='utf-8')"
            ]
        },
        {
            "label": "py-arg-write-test2",
            "type": "shell",
            "command": "python",
            "args": [
                "-c",
                "from pathlib import Path; Path(\"tmp_py_arg_test.txt\").write_text(\"\\\"abc\\\"\\n\", encoding=\"utf-8\")"
            ]
        },
        {
            "label": "rewrite-run-bundle-lines",
            "type": "shell",
            "command": "$lines = @(\n'#!/usr/bin/env python3',\n'\"\"\"Managed parse runner for Flame/FFR workflows.',\n'',\n'Creates one output folder per source, and appends a timestamp if the same',\n'source folder already exists.',\n'\"\"\"',\n'',\n'from __future__ import annotations',\n'',\n'import argparse',\n'import json',\n'import re',\n'import subprocess',\n'import sys',\n'from datetime import datetime, timezone',\n'from pathlib import Path',\n'from typing import Dict, List, Optional',\n'',\n'',\n'def slugify(value: str) -> str:',\n'    cleaned = re.sub(r\"[^A-Za-z0-9._-]+\", \"_\", value.strip())',\n'    cleaned = re.sub(r\"_+\", \"_\", cleaned).strip(\"._-\")',\n'    return cleaned or \"source\"',\n'',\n'',\n'def create_run_dir(output_root: Path, source_label: str) -> Path:',\n'    output_root.mkdir(parents=True, exist_ok=True)',\n'    base = slugify(source_label)',\n'    target = output_root / base',\n'    if target.exists():',\n'        stamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")',\n'        target = output_root / f\"{base}_{stamp}\"',\n'    target.mkdir(parents=True, exist_ok=False)',\n'    return target',\n'',\n'',\n'def run_cmd(cmd: List[str]) -> None:',\n'    proc = subprocess.run(cmd, text=True)',\n'    if proc.returncode != 0:',\n'        raise SystemExit(proc.returncode)',\n'',\n'',\n'def resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:',\n'    if not raw:',\n'        return None',\n'    path = Path(raw)',\n'    return str(path.resolve()) if path.exists() else None',\n'',\n'',\n'def write_run_metadata(',\n'    run_dir: Path,',\n'    args: argparse.Namespace,',\n'    generated: Dict[str, str],',\n'    repo_resolved: Optional[Path],',\n'    qdf_dir_used: Optional[str],',\n'    lineitem_report_used: Optional[str],',\n') -> Path:',\n'    source_path = Path(args.source)',\n'    source_resolved = str(source_path.resolve()) if source_path.exists() else None',\n'',\n'    flame_repos: List[str] = []',\n'    if repo_resolved is not None:',\n'        flame_repos.append(str(repo_resolved))',\n'',\n'    payload = {',\n'        \"created_utc\": datetime.now(timezone.utc).isoformat(),',\n'        \"run_dir\": str(run_dir),',\n'        \"workflow_type\": args.type,',\n'        \"compat_mode\": args.compat_mode,',\n'        \"ffr_source\": {',\n'            \"input\": args.source,',\n'            \"resolved\": source_resolved,',\n'        },',\n'        \"flame_git_repos_parsed\": flame_repos,',\n'        \"inputs\": {',\n'            \"repo_input\": args.repo,',\n'            \"repo_resolved\": str(repo_resolved) if repo_resolved else None,',\n'            \"lineitem_report\": lineitem_report_used,',\n'            \"lineitem_report_resolved\": resolved_path_if_exists(lineitem_report_used),',\n'            \"qdf_dir\": qdf_dir_used,',\n'            \"qdf_dir_resolved\": resolved_path_if_exists(qdf_dir_used),',\n'        },',\n'        \"files\": generated,',\n'    }',\n'',\n'    metadata_path = run_dir / \"run_metadata.json\"',\n'    metadata_path.write_text(json.dumps(payload, indent=2), encoding=\"utf-8\")',\n'    return metadata_path',\n'',\n'',\n'def parse_args() -> argparse.Namespace:',\n'    parser = argparse.ArgumentParser(description=\"Run lineitem/qdf parsers with managed output folders\")',\n'    parser.add_argument(\"--source\", required=True, help=\"Source identifier (FFR path or repo path/URL)\")',\n'    parser.add_argument(\"--type\", choices=[\"lineitem\", \"qdf\", \"both\"], default=\"both\")',\n'    parser.add_argument(\"--repo\", help=\"Repo path for lineitem parser (defaults to --source)\")',\n'    parser.add_argument(\"--lineitem-report\", help=\"LineItem CSV for qdf mode (required for qdf-only unless compat ffr default exists)\")',\n'    parser.add_argument(\"--qdf-dir\", help=\"QDF JSON directory for qdf mode\")',\n'    parser.add_argument(\"--compat-mode\", choices=[\"ffr\"], help=\"Optional compat mode for defaults\")',\n'    parser.add_argument(\"--output-root\", default=\"out/runs\", help=\"Root folder where managed run dirs are created\")',\n'    parser.add_argument(\"--label\", help=\"Optional custom folder label\")',\n'    return parser.parse_args()',\n'',\n'',\n'def main() -> int:',\n'    args = parse_args()',\n'    source_label = args.label or Path(args.source).name or args.source',\n'    run_dir = create_run_dir(Path(args.output_root).resolve(), source_label)',\n'',\n'    python_exe = sys.executable',\n'    generated: Dict[str, str] = {}',\n'',\n'    lineitem_csv = run_dir / \"LineItemAttributes_Report.csv\"',\n'    lineitem_json = run_dir / \"lineitem_attributes.json\"',\n'',\n'    repo_resolved: Optional[Path] = None',\n'    qdf_dir_used: Optional[str] = None',\n'    lineitem_report_used: Optional[str] = None',\n'',\n'    if args.type in {\"lineitem\", \"both\"}:',\n'        repo_resolved = Path(args.repo or args.source).resolve()',\n'        cmd = [',\n'            python_exe,',\n'            \"scripts/lineitem_attribute_parser.py\",',\n'            \"--repo\",',\n'            str(repo_resolved),',\n'            \"--output-csv\",',\n'            str(lineitem_csv),',\n'            \"--output-json\",',\n'            str(lineitem_json),',\n'        ]',\n'        run_cmd(cmd)',\n'        generated[\"lineitem_csv\"] = str(lineitem_csv)',\n'        generated[\"lineitem_json\"] = str(lineitem_json)',\n'',\n'    if args.type in {\"qdf\", \"both\"}:',\n'        lineitem_report = args.lineitem_report',\n'        if not lineitem_report and args.type == \"both\":',\n'            lineitem_report = str(lineitem_csv)',\n'        if not lineitem_report and args.compat_mode == \"ffr\":',\n'            lineitem_report = \"out/ffr/lineitem_attributes_from_ffr.csv\"',\n'        if not lineitem_report:',\n'            raise SystemExit(\"--lineitem-report is required for qdf mode (or use --type both)\")',\n'',\n'        qdf_dir = args.qdf_dir or (\"out/ffr/qdf-json\" if args.compat_mode == \"ffr\" else None)',\n'        if not qdf_dir:',\n'            raise SystemExit(\"--qdf-dir is required for qdf mode (or use --compat-mode ffr)\")',\n'',\n'        lineitem_report_used = str(lineitem_report)',\n'        qdf_dir_used = str(qdf_dir)',\n'',\n'        qdf_csv = run_dir / \"qdf_validation_from_ffr.csv\"',\n'        qdf_json = run_dir / \"qdf_validation_from_ffr.json\"',\n'        heatmap_csv = run_dir / \"qdf_validation_heatmap.csv\"',\n'        heatmap_json = run_dir / \"qdf_validation_heatmap.json\"',\n'        heatmap_xlsx = run_dir / \"QDF_LIRA_Validation_Heatmap.xlsx\"',\n'',\n'        cmd = [',\n'            python_exe,',\n'            \"scripts/qdf_validation_parser.py\",',\n'            \"--lineitem-report\",',\n'            str(lineitem_report),',\n'            \"--qdf-dir\",',\n'            str(qdf_dir),',\n'            \"--output-csv\",',\n'            str(qdf_csv),',\n'            \"--output-json\",',\n'            str(qdf_json),',\n'            \"--output-heatmap-csv\",',\n'            str(heatmap_csv),',\n'            \"--output-heatmap-json\",',\n'            str(heatmap_json),',\n'            \"--output-heatmap-xlsx\",',\n'            str(heatmap_xlsx),',\n'        ]',\n'        run_cmd(cmd)',\n'        generated.update(',\n'            {',\n'                \"qdf_csv\": str(qdf_csv),',\n'                \"qdf_json\": str(qdf_json),',\n'                \"heatmap_csv\": str(heatmap_csv),',\n'                \"heatmap_json\": str(heatmap_json),',\n'                \"heatmap_xlsx\": str(heatmap_xlsx),',\n'            }',\n'        )',\n'',\n'    metadata_path = write_run_metadata(',\n'        run_dir=run_dir,',\n'        args=args,',\n'        generated=generated,',\n'        repo_resolved=repo_resolved,',\n'        qdf_dir_used=qdf_dir_used,',\n'        lineitem_report_used=lineitem_report_used,',\n'    )',\n'    generated[\"run_metadata_json\"] = str(metadata_path)',\n'',\n'    print(json.dumps({\"run_dir\": str(run_dir), \"files\": generated}, indent=2))',\n'    return 0',\n'',\n'',\n'if __name__ == \"__main__\":',\n'    raise SystemExit(main())'\n);\nSet-Content -Path 'scripts/run_parse_bundle.py' -Value $lines -Encoding utf8"
        },
        {
            "label": "compile-run-bundle-4",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "quote-survival-test",
            "type": "shell",
            "command": "$lines=@('x = ''abc''','y = \"def\"'); Set-Content -Path 'tmp_quote_test.py' -Value $lines -Encoding utf8"
        },
        {
            "label": "rewrite-run-bundle-singlequote",
            "type": "shell",
            "command": "$lines=@(\n'#!/usr/bin/env python3',\n'''''Managed parse runner for Flame/FFR workflows.',\n'',\n'Creates one output folder per source, and appends a timestamp if the same',\n'source folder already exists.',\n''''''',\n'',\n'from __future__ import annotations',\n'',\n'import argparse',\n'import json',\n'import re',\n'import subprocess',\n'import sys',\n'from datetime import datetime, timezone',\n'from pathlib import Path',\n'from typing import Dict, List, Optional',\n'',\n'',\n'def slugify(value: str) -> str:',\n'    cleaned = re.sub(r''[^A-Za-z0-9._-]+'', ''_'', value.strip())',\n'    cleaned = re.sub(r''_+'', ''_'', cleaned).strip(''._-'')',\n'    return cleaned or ''source''',\n'',\n'',\n'def create_run_dir(output_root: Path, source_label: str) -> Path:',\n'    output_root.mkdir(parents=True, exist_ok=True)',\n'    base = slugify(source_label)',\n'    target = output_root / base',\n'    if target.exists():',\n'        stamp = datetime.now().strftime(''%Y%m%d_%H%M%S'')',\n'        target = output_root / f''{base}_{stamp}''',\n'    target.mkdir(parents=True, exist_ok=False)',\n'    return target',\n'',\n'',\n'def run_cmd(cmd: List[str]) -> None:',\n'    proc = subprocess.run(cmd, text=True)',\n'    if proc.returncode != 0:',\n'        raise SystemExit(proc.returncode)',\n'',\n'',\n'def resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:',\n'    if not raw:',\n'        return None',\n'    path = Path(raw)',\n'    return str(path.resolve()) if path.exists() else None',\n'',\n'',\n'def write_run_metadata(',\n'    run_dir: Path,',\n'    args: argparse.Namespace,',\n'    generated: Dict[str, str],',\n'    repo_resolved: Optional[Path],',\n'    qdf_dir_used: Optional[str],',\n'    lineitem_report_used: Optional[str],',\n') -> Path:',\n'    source_path = Path(args.source)',\n'    source_resolved = str(source_path.resolve()) if source_path.exists() else None',\n'',\n'    flame_repos: List[str] = []',\n'    if repo_resolved is not None:',\n'        flame_repos.append(str(repo_resolved))',\n'',\n'    payload = {',\n'        ''created_utc'': datetime.now(timezone.utc).isoformat(),',\n'        ''run_dir'': str(run_dir),',\n'        ''workflow_type'': args.type,',\n'        ''compat_mode'': args.compat_mode,',\n'        ''ffr_source'': {',\n'            ''input'': args.source,',\n'            ''resolved'': source_resolved,',\n'        },',\n'        ''flame_git_repos_parsed'': flame_repos,',\n'        ''inputs'': {',\n'            ''repo_input'': args.repo,',\n'            ''repo_resolved'': str(repo_resolved) if repo_resolved else None,',\n'            ''lineitem_report'': lineitem_report_used,',\n'            ''lineitem_report_resolved'': resolved_path_if_exists(lineitem_report_used),',\n'            ''qdf_dir'': qdf_dir_used,',\n'            ''qdf_dir_resolved'': resolved_path_if_exists(qdf_dir_used),',\n'        },',\n'        ''files'': generated,',\n'    }',\n'',\n'    metadata_path = run_dir / ''run_metadata.json''',\n'    metadata_path.write_text(json.dumps(payload, indent=2), encoding=''utf-8'')',\n'    return metadata_path',\n'',\n'',\n'def parse_args() -> argparse.Namespace:',\n'    parser = argparse.ArgumentParser(description=''Run lineitem/qdf parsers with managed output folders'')',\n'    parser.add_argument(''--source'', required=True, help=''Source identifier (FFR path or repo path/URL)'')',\n'    parser.add_argument(''--type'', choices=[''lineitem'', ''qdf'', ''both''], default=''both'')',\n'    parser.add_argument(''--repo'', help=''Repo path for lineitem parser (defaults to --source)'')',\n'    parser.add_argument(''--lineitem-report'', help=''LineItem CSV for qdf mode (required for qdf-only unless compat ffr default exists)'')',\n'    parser.add_argument(''--qdf-dir'', help=''QDF JSON directory for qdf mode'')',\n'    parser.add_argument(''--compat-mode'', choices=[''ffr''], help=''Optional compat mode for defaults'')',\n'    parser.add_argument(''--output-root'', default=''out/runs'', help=''Root folder where managed run dirs are created'')',\n'    parser.add_argument(''--label'', help=''Optional custom folder label'')',\n'    return parser.parse_args()',\n'',\n'',\n'def main() -> int:',\n'    args = parse_args()',\n'    source_label = args.label or Path(args.source).name or args.source',\n'    run_dir = create_run_dir(Path(args.output_root).resolve(), source_label)',\n'',\n'    python_exe = sys.executable',\n'    generated: Dict[str, str] = {}',\n'',\n'    lineitem_csv = run_dir / ''LineItemAttributes_Report.csv''',\n'    lineitem_json = run_dir / ''lineitem_attributes.json''',\n'',\n'    repo_resolved: Optional[Path] = None',\n'    qdf_dir_used: Optional[str] = None',\n'    lineitem_report_used: Optional[str] = None',\n'',\n'    if args.type in {''lineitem'', ''both''}:',\n'        repo_resolved = Path(args.repo or args.source).resolve()',\n'        cmd = [',\n'            python_exe,',\n'            ''scripts/lineitem_attribute_parser.py'',',\n'            ''--repo'',',\n'            str(repo_resolved),',\n'            ''--output-csv'',',\n'            str(lineitem_csv),',\n'            ''--output-json'',',\n'            str(lineitem_json),',\n'        ]',\n'        run_cmd(cmd)',\n'        generated[''lineitem_csv''] = str(lineitem_csv)',\n'        generated[''lineitem_json''] = str(lineitem_json)',\n'',\n'    if args.type in {''qdf'', ''both''}:',\n'        lineitem_report = args.lineitem_report',\n'        if not lineitem_report and args.type == ''both'':',\n'            lineitem_report = str(lineitem_csv)',\n'        if not lineitem_report and args.compat_mode == ''ffr'':',\n'            lineitem_report = ''out/ffr/lineitem_attributes_from_ffr.csv''',\n'        if not lineitem_report:',\n'            raise SystemExit(''--lineitem-report is required for qdf mode (or use --type both)'')',\n'',\n'        qdf_dir = args.qdf_dir or (''out/ffr/qdf-json'' if args.compat_mode == ''ffr'' else None)',\n'        if not qdf_dir:',\n'            raise SystemExit(''--qdf-dir is required for qdf mode (or use --compat-mode ffr)'')',\n'',\n'        lineitem_report_used = str(lineitem_report)',\n'        qdf_dir_used = str(qdf_dir)',\n'',\n'        qdf_csv = run_dir / ''qdf_validation_from_ffr.csv''',\n'        qdf_json = run_dir / ''qdf_validation_from_ffr.json''',\n'        heatmap_csv = run_dir / ''qdf_validation_heatmap.csv''',\n'        heatmap_json = run_dir / ''qdf_validation_heatmap.json''',\n'        heatmap_xlsx = run_dir / ''QDF_LIRA_Validation_Heatmap.xlsx''',\n'',\n'        cmd = [',\n'            python_exe,',\n'            ''scripts/qdf_validation_parser.py'',',\n'            ''--lineitem-report'',',\n'            str(lineitem_report),',\n'            ''--qdf-dir'',',\n'            str(qdf_dir),',\n'            ''--output-csv'',',\n'            str(qdf_csv),',\n'            ''--output-json'',',\n'            str(qdf_json),',\n'            ''--output-heatmap-csv'',',\n'            str(heatmap_csv),',\n'            ''--output-heatmap-json'',',\n'            str(heatmap_json),',\n'            ''--output-heatmap-xlsx'',',\n'            str(heatmap_xlsx),',\n'        ]',\n'        run_cmd(cmd)',\n'        generated.update(',\n'            {',\n'                ''qdf_csv'': str(qdf_csv),',\n'                ''qdf_json'': str(qdf_json),',\n'                ''heatmap_csv'': str(heatmap_csv),',\n'                ''heatmap_json'': str(heatmap_json),',\n'                ''heatmap_xlsx'': str(heatmap_xlsx),',\n'            }',\n'        )',\n'',\n'    metadata_path = write_run_metadata(',\n'        run_dir=run_dir,',\n'        args=args,',\n'        generated=generated,',\n'        repo_resolved=repo_resolved,',\n'        qdf_dir_used=qdf_dir_used,',\n'        lineitem_report_used=lineitem_report_used,',\n'    )',\n'    generated[''run_metadata_json''] = str(metadata_path)',\n'',\n'    print(json.dumps({''run_dir'': str(run_dir), ''files'': generated}, indent=2))',\n'    return 0',\n'',\n'',\n'if __name__ == ''__main__'':',\n'    raise SystemExit(main())'\n);\nSet-Content -Path 'scripts/run_parse_bundle.py' -Value $lines -Encoding utf8"
        },
        {
            "label": "compile-run-bundle-5",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "restore-run-bundle-from-head",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "git show HEAD:scripts/run_parse_bundle.py > scripts\\run_parse_bundle.py"
            ]
        },
        {
            "label": "single-quote-style-test",
            "type": "shell",
            "command": "$lines=@('a = ''x''','b = re.sub(r''_+'', ''_'', ''abc'')'); Set-Content -Path 'tmp_sq_style.py' -Value $lines -Encoding utf8"
        },
        {
            "label": "rewrite-run-bundle-clean",
            "type": "shell",
            "command": "$lines=@(\n'#!/usr/bin/env python3',\n'from __future__ import annotations',\n'',\n'import argparse',\n'import json',\n'import re',\n'import subprocess',\n'import sys',\n'from datetime import datetime, timezone',\n'from pathlib import Path',\n'from typing import Dict, List, Optional',\n'',\n'',\n'def slugify(value: str) -> str:',\n'    cleaned = re.sub(r''[^A-Za-z0-9._-]+'', ''_'', value.strip())',\n'    cleaned = re.sub(r''_+'', ''_'', cleaned).strip(''._-'')',\n'    return cleaned or ''source''',\n'',\n'',\n'def create_run_dir(output_root: Path, source_label: str) -> Path:',\n'    output_root.mkdir(parents=True, exist_ok=True)',\n'    base = slugify(source_label)',\n'    target = output_root / base',\n'    if target.exists():',\n'        stamp = datetime.now().strftime(''%Y%m%d_%H%M%S'')',\n'        target = output_root / f''{base}_{stamp}''',\n'    target.mkdir(parents=True, exist_ok=False)',\n'    return target',\n'',\n'',\n'def run_cmd(cmd: List[str]) -> None:',\n'    proc = subprocess.run(cmd, text=True)',\n'    if proc.returncode != 0:',\n'        raise SystemExit(proc.returncode)',\n'',\n'',\n'def resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:',\n'    if not raw:',\n'        return None',\n'    path = Path(raw)',\n'    return str(path.resolve()) if path.exists() else None',\n'',\n'',\n'def write_run_metadata(',\n'    run_dir: Path,',\n'    args: argparse.Namespace,',\n'    generated: Dict[str, str],',\n'    repo_resolved: Optional[Path],',\n'    qdf_dir_used: Optional[str],',\n'    lineitem_report_used: Optional[str],',\n') -> Path:',\n'    source_path = Path(args.source)',\n'    source_resolved = str(source_path.resolve()) if source_path.exists() else None',\n'',\n'    flame_repos: List[str] = []',\n'    if repo_resolved is not None:',\n'        flame_repos.append(str(repo_resolved))',\n'',\n'    payload = {',\n'        ''created_utc'': datetime.now(timezone.utc).isoformat(),',\n'        ''run_dir'': str(run_dir),',\n'        ''workflow_type'': args.type,',\n'        ''compat_mode'': args.compat_mode,',\n'        ''ffr_source'': {',\n'            ''input'': args.source,',\n'            ''resolved'': source_resolved,',\n'        },',\n'        ''flame_git_repos_parsed'': flame_repos,',\n'        ''inputs'': {',\n'            ''repo_input'': args.repo,',\n'            ''repo_resolved'': str(repo_resolved) if repo_resolved else None,',\n'            ''lineitem_report'': lineitem_report_used,',\n'            ''lineitem_report_resolved'': resolved_path_if_exists(lineitem_report_used),',\n'            ''qdf_dir'': qdf_dir_used,',\n'            ''qdf_dir_resolved'': resolved_path_if_exists(qdf_dir_used),',\n'        },',\n'        ''files'': generated,',\n'    }',\n'',\n'    metadata_path = run_dir / ''run_metadata.json''',\n'    metadata_path.write_text(json.dumps(payload, indent=2), encoding=''utf-8'')',\n'    return metadata_path',\n'',\n'',\n'def parse_args() -> argparse.Namespace:',\n'    parser = argparse.ArgumentParser(description=''Run lineitem/qdf parsers with managed output folders'')',\n'    parser.add_argument(''--source'', required=True, help=''Source identifier (FFR path or repo path/URL)'')',\n'    parser.add_argument(''--type'', choices=[''lineitem'', ''qdf'', ''both''], default=''both'')',\n'    parser.add_argument(''--repo'', help=''Repo path for lineitem parser (defaults to --source)'')',\n'    parser.add_argument(''--lineitem-report'', help=''LineItem CSV for qdf mode (required for qdf-only unless compat ffr default exists)'')',\n'    parser.add_argument(''--qdf-dir'', help=''QDF JSON directory for qdf mode'')',\n'    parser.add_argument(''--compat-mode'', choices=[''ffr''], help=''Optional compat mode for defaults'')',\n'    parser.add_argument(''--output-root'', default=''out/runs'', help=''Root folder where managed run dirs are created'')',\n'    parser.add_argument(''--label'', help=''Optional custom folder label'')',\n'    return parser.parse_args()',\n'',\n'',\n'def main() -> int:',\n'    args = parse_args()',\n'    source_label = args.label or Path(args.source).name or args.source',\n'    run_dir = create_run_dir(Path(args.output_root).resolve(), source_label)',\n'',\n'    python_exe = sys.executable',\n'    generated: Dict[str, str] = {}',\n'',\n'    lineitem_csv = run_dir / ''LineItemAttributes_Report.csv''',\n'    lineitem_json = run_dir / ''lineitem_attributes.json''',\n'',\n'    repo_resolved: Optional[Path] = None',\n'    qdf_dir_used: Optional[str] = None',\n'    lineitem_report_used: Optional[str] = None',\n'',\n'    if args.type in {''lineitem'', ''both''}:',\n'        repo_resolved = Path(args.repo or args.source).resolve()',\n'        cmd = [',\n'            python_exe,',\n'            ''scripts/lineitem_attribute_parser.py'',',\n'            ''--repo'',',\n'            str(repo_resolved),',\n'            ''--output-csv'',',\n'            str(lineitem_csv),',\n'            ''--output-json'',',\n'            str(lineitem_json),',\n'        ]',\n'        run_cmd(cmd)',\n'        generated[''lineitem_csv''] = str(lineitem_csv)',\n'        generated[''lineitem_json''] = str(lineitem_json)',\n'',\n'    if args.type in {''qdf'', ''both''}:',\n'        lineitem_report = args.lineitem_report',\n'        if not lineitem_report and args.type == ''both'':',\n'            lineitem_report = str(lineitem_csv)',\n'        if not lineitem_report and args.compat_mode == ''ffr'':',\n'            lineitem_report = ''out/ffr/lineitem_attributes_from_ffr.csv''',\n'        if not lineitem_report:',\n'            raise SystemExit(''--lineitem-report is required for qdf mode (or use --type both)'')',\n'',\n'        qdf_dir = args.qdf_dir or (''out/ffr/qdf-json'' if args.compat_mode == ''ffr'' else None)',\n'        if not qdf_dir:',\n'            raise SystemExit(''--qdf-dir is required for qdf mode (or use --compat-mode ffr)'')',\n'',\n'        lineitem_report_used = str(lineitem_report)',\n'        qdf_dir_used = str(qdf_dir)',\n'',\n'        qdf_csv = run_dir / ''qdf_validation_from_ffr.csv''',\n'        qdf_json = run_dir / ''qdf_validation_from_ffr.json''',\n'        heatmap_csv = run_dir / ''qdf_validation_heatmap.csv''',\n'        heatmap_json = run_dir / ''qdf_validation_heatmap.json''',\n'        heatmap_xlsx = run_dir / ''QDF_LIRA_Validation_Heatmap.xlsx''',\n'',\n'        cmd = [',\n'            python_exe,',\n'            ''scripts/qdf_validation_parser.py'',',\n'            ''--lineitem-report'',',\n'            str(lineitem_report),',\n'            ''--qdf-dir'',',\n'            str(qdf_dir),',\n'            ''--output-csv'',',\n'            str(qdf_csv),',\n'            ''--output-json'',',\n'            str(qdf_json),',\n'            ''--output-heatmap-csv'',',\n'            str(heatmap_csv),',\n'            ''--output-heatmap-json'',',\n'            str(heatmap_json),',\n'            ''--output-heatmap-xlsx'',',\n'            str(heatmap_xlsx),',\n'        ]',\n'        run_cmd(cmd)',\n'        generated.update(',\n'            {',\n'                ''qdf_csv'': str(qdf_csv),',\n'                ''qdf_json'': str(qdf_json),',\n'                ''heatmap_csv'': str(heatmap_csv),',\n'                ''heatmap_json'': str(heatmap_json),',\n'                ''heatmap_xlsx'': str(heatmap_xlsx),',\n'            }',\n'        )',\n'',\n'    metadata_path = write_run_metadata(',\n'        run_dir=run_dir,',\n'        args=args,',\n'        generated=generated,',\n'        repo_resolved=repo_resolved,',\n'        qdf_dir_used=qdf_dir_used,',\n'        lineitem_report_used=lineitem_report_used,',\n'    )',\n'    generated[''run_metadata_json''] = str(metadata_path)',\n'',\n'    print(json.dumps({''run_dir'': str(run_dir), ''files'': generated}, indent=2))',\n'    return 0',\n'',\n'',\n'if __name__ == ''__main__'':',\n'    raise SystemExit(main())'\n); Set-Content -Path 'scripts/run_parse_bundle.py' -Value $lines -Encoding utf8"
        },
        {
            "label": "compile-run-bundle-6",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "show-indent-run-bundle",
            "type": "shell",
            "command": "$i=1; Get-Content scripts/run_parse_bundle.py | ForEach-Object { if ($i -ge 20 -and $i -le 30) { Write-Output ($i.ToString() + ':[' + ($_ -replace ' ','\u00c2\u00b7') + ']') }; $i++ }"
        },
        {
            "label": "tab-indent-test",
            "type": "shell",
            "command": "$lines=@(\"if True:\",\"`tprint('a')\",\"`tif True:\",\"`t`tprint('b')\"); Set-Content -Path 'tmp_tab_test.py' -Value $lines -Encoding utf8; python -m py_compile tmp_tab_test.py"
        },
        {
            "label": "rewrite-run-bundle-markers",
            "type": "shell",
            "command": "$lines=@(\n'#!/usr/bin/env python3',\n'from __future__ import annotations',\n'',\n'import argparse',\n'import json',\n'import re',\n'import subprocess',\n'import sys',\n'from datetime import datetime, timezone',\n'from pathlib import Path',\n'from typing import Dict, List, Optional',\n'',\n'def slugify(value: str) -> str:',\n'>cleaned = re.sub(r''[^A-Za-z0-9._-]+'', ''_'', value.strip())',\n'>cleaned = re.sub(r''_+'', ''_'', cleaned).strip(''._-'')',\n'>return cleaned or ''source''',\n'',\n'def create_run_dir(output_root: Path, source_label: str) -> Path:',\n'>output_root.mkdir(parents=True, exist_ok=True)',\n'>base = slugify(source_label)',\n'>target = output_root / base',\n'>if target.exists():',\n'>>stamp = datetime.now().strftime(''%Y%m%d_%H%M%S'')',\n'>>target = output_root / f''{base}_{stamp}''',\n'>target.mkdir(parents=True, exist_ok=False)',\n'>return target',\n'',\n'def run_cmd(cmd: List[str]) -> None:',\n'>proc = subprocess.run(cmd, text=True)',\n'>if proc.returncode != 0:',\n'>>raise SystemExit(proc.returncode)',\n'',\n'def resolved_path_if_exists(raw: Optional[str]) -> Optional[str]:',\n'>if not raw:',\n'>>return None',\n'>path = Path(raw)',\n'>return str(path.resolve()) if path.exists() else None',\n'',\n'def write_run_metadata(',\n'>run_dir: Path,',\n'>args: argparse.Namespace,',\n'>generated: Dict[str, str],',\n'>repo_resolved: Optional[Path],',\n'>qdf_dir_used: Optional[str],',\n'>lineitem_report_used: Optional[str],',\n') -> Path:',\n'>source_path = Path(args.source)',\n'>source_resolved = str(source_path.resolve()) if source_path.exists() else None',\n'>flame_repos: List[str] = []',\n'>if repo_resolved is not None:',\n'>>flame_repos.append(str(repo_resolved))',\n'>payload = {',\n'>>''created_utc'': datetime.now(timezone.utc).isoformat(),',\n'>>''run_dir'': str(run_dir),',\n'>>''workflow_type'': args.type,',\n'>>''compat_mode'': args.compat_mode,',\n'>>''ffr_source'': {',\n'>>>''input'': args.source,',\n'>>>''resolved'': source_resolved,',\n'>>},',\n'>>''flame_git_repos_parsed'': flame_repos,',\n'>>''inputs'': {',\n'>>>''repo_input'': args.repo,',\n'>>>''repo_resolved'': str(repo_resolved) if repo_resolved else None,',\n'>>>''lineitem_report'': lineitem_report_used,',\n'>>>''lineitem_report_resolved'': resolved_path_if_exists(lineitem_report_used),',\n'>>>''qdf_dir'': qdf_dir_used,',\n'>>>''qdf_dir_resolved'': resolved_path_if_exists(qdf_dir_used),',\n'>>},',\n'>>''files'': generated,',\n'>}',\n'>metadata_path = run_dir / ''run_metadata.json''',\n'>metadata_path.write_text(json.dumps(payload, indent=2), encoding=''utf-8'')',\n'>return metadata_path',\n'',\n'def parse_args() -> argparse.Namespace:',\n'>parser = argparse.ArgumentParser(description=''Run lineitem/qdf parsers with managed output folders'')',\n'>parser.add_argument(''--source'', required=True, help=''Source identifier (FFR path or repo path/URL)'')',\n'>parser.add_argument(''--type'', choices=[''lineitem'', ''qdf'', ''both''], default=''both'')',\n'>parser.add_argument(''--repo'', help=''Repo path for lineitem parser (defaults to --source)'')',\n'>parser.add_argument(''--lineitem-report'', help=''LineItem CSV for qdf mode (required for qdf-only unless compat ffr default exists)'')',\n'>parser.add_argument(''--qdf-dir'', help=''QDF JSON directory for qdf mode'')',\n'>parser.add_argument(''--compat-mode'', choices=[''ffr''], help=''Optional compat mode for defaults'')',\n'>parser.add_argument(''--output-root'', default=''out/runs'', help=''Root folder where managed run dirs are created'')',\n'>parser.add_argument(''--label'', help=''Optional custom folder label'')',\n'>return parser.parse_args()',\n'',\n'def main() -> int:',\n'>args = parse_args()',\n'>source_label = args.label or Path(args.source).name or args.source',\n'>run_dir = create_run_dir(Path(args.output_root).resolve(), source_label)',\n'>python_exe = sys.executable',\n'>generated: Dict[str, str] = {}',\n'>lineitem_csv = run_dir / ''LineItemAttributes_Report.csv''',\n'>lineitem_json = run_dir / ''lineitem_attributes.json''',\n'>repo_resolved: Optional[Path] = None',\n'>qdf_dir_used: Optional[str] = None',\n'>lineitem_report_used: Optional[str] = None',\n'>if args.type in {''lineitem'', ''both''}:',\n'>>repo_resolved = Path(args.repo or args.source).resolve()',\n'>>cmd = [',\n'>>>python_exe,',\n'>>>''scripts/lineitem_attribute_parser.py'',',\n'>>>''--repo'',',\n'>>>str(repo_resolved),',\n'>>>''--output-csv'',',\n'>>>str(lineitem_csv),',\n'>>>''--output-json'',',\n'>>>str(lineitem_json),',\n'>>]',\n'>>run_cmd(cmd)',\n'>>generated[''lineitem_csv''] = str(lineitem_csv)',\n'>>generated[''lineitem_json''] = str(lineitem_json)',\n'>if args.type in {''qdf'', ''both''}:',\n'>>lineitem_report = args.lineitem_report',\n'>>if not lineitem_report and args.type == ''both'':',\n'>>>lineitem_report = str(lineitem_csv)',\n'>>if not lineitem_report and args.compat_mode == ''ffr'':',\n'>>>lineitem_report = ''out/ffr/lineitem_attributes_from_ffr.csv''',\n'>>if not lineitem_report:',\n'>>>raise SystemExit(''--lineitem-report is required for qdf mode (or use --type both)'')',\n'>>qdf_dir = args.qdf_dir or (''out/ffr/qdf-json'' if args.compat_mode == ''ffr'' else None)',\n'>>if not qdf_dir:',\n'>>>raise SystemExit(''--qdf-dir is required for qdf mode (or use --compat-mode ffr)'')',\n'>>lineitem_report_used = str(lineitem_report)',\n'>>qdf_dir_used = str(qdf_dir)',\n'>>qdf_csv = run_dir / ''qdf_validation_from_ffr.csv''',\n'>>qdf_json = run_dir / ''qdf_validation_from_ffr.json''',\n'>>heatmap_csv = run_dir / ''qdf_validation_heatmap.csv''',\n'>>heatmap_json = run_dir / ''qdf_validation_heatmap.json''',\n'>>heatmap_xlsx = run_dir / ''QDF_LIRA_Validation_Heatmap.xlsx''',\n'>>cmd = [',\n'>>>python_exe,',\n'>>>''scripts/qdf_validation_parser.py'',',\n'>>>''--lineitem-report'',',\n'>>>str(lineitem_report),',\n'>>>''--qdf-dir'',',\n'>>>str(qdf_dir),',\n'>>>''--output-csv'',',\n'>>>str(qdf_csv),',\n'>>>''--output-json'',',\n'>>>str(qdf_json),',\n'>>>''--output-heatmap-csv'',',\n'>>>str(heatmap_csv),',\n'>>>''--output-heatmap-json'',',\n'>>>str(heatmap_json),',\n'>>>''--output-heatmap-xlsx'',',\n'>>>str(heatmap_xlsx),',\n'>>]',\n'>>run_cmd(cmd)',\n'>>generated.update({',\n'>>>''qdf_csv'': str(qdf_csv),',\n'>>>''qdf_json'': str(qdf_json),',\n'>>>''heatmap_csv'': str(heatmap_csv),',\n'>>>''heatmap_json'': str(heatmap_json),',\n'>>>''heatmap_xlsx'': str(heatmap_xlsx),',\n'>>})',\n'>metadata_path = write_run_metadata(',\n'>>run_dir=run_dir,',\n'>>args=args,',\n'>>generated=generated,',\n'>>repo_resolved=repo_resolved,',\n'>>qdf_dir_used=qdf_dir_used,',\n'>>lineitem_report_used=lineitem_report_used,',\n'>)',\n'>generated[''run_metadata_json''] = str(metadata_path)',\n'>print(json.dumps({''run_dir'': str(run_dir), ''files'': generated}, indent=2))',\n'>return 0',\n'',\n'if __name__ == ''__main__'':',\n'>raise SystemExit(main())'\n);\nSet-Content -Path 'scripts/run_parse_bundle.py' -Value $lines -Encoding utf8;\n$t = Get-Content -Raw -Path 'scripts/run_parse_bundle.py';\n$t = [regex]::Replace($t,'(?m)^(>+)', { param($m) (' ' * $m.Value.Length) });\nSet-Content -Path 'scripts/run_parse_bundle.py' -Value $t -Encoding utf8"
        },
        {
            "label": "compile-run-bundle-7",
            "type": "shell",
            "command": "python -m py_compile scripts/run_parse_bundle.py"
        },
        {
            "label": "patch-flame-agent-managed-output",
            "type": "shell",
            "command": "$p='.github/agents/flame-repo-parser.agent.md'; $t=Get-Content -Raw -Path $p; if (-not $t.Contains('run_metadata.json')) { $insert='- For LineItem/QDF artifact-producing flows, ALWAYS orchestrate execution through scripts/run_parse_bundle.py so outputs are created under source-managed run folders with timestamp collision handling.`r`n- Ensure each generated run folder contains run_metadata.json that records the FFR source input/resolved path and the Flame git repo(s) parsed.'; $anchor='- If user intent includes FuseGen decoded value extraction from ReadOnly folders, ALWAYS delegate extraction to the `FuseGen ReadOnly Parser` subagent.'; if ($t.Contains($anchor)) { $t=$t.Replace($anchor, $anchor + \"`r`n\" + $insert) } }; Set-Content -Path $p -Value $t -Encoding utf8"
        },
        {
            "label": "patch-flame-agent-managed-output-2",
            "type": "shell",
            "command": "$p='.github/agents/flame-repo-parser.agent.md'; $lines=Get-Content -Path $p; if (-not ($lines -match 'run_metadata.json')) { $out=@(); foreach ($line in $lines) { $out += $line; if ($line -eq '- If user intent includes FuseGen decoded value extraction from ReadOnly folders, ALWAYS delegate extraction to the `FuseGen ReadOnly Parser` subagent.') { $out += '- For LineItem/QDF artifact-producing flows, ALWAYS orchestrate execution through scripts/run_parse_bundle.py so outputs are created under source-managed run folders with timestamp collision handling.'; $out += '- Ensure each generated run folder contains run_metadata.json that records the FFR source input/resolved path and the Flame git repo(s) parsed.' } }; Set-Content -Path $p -Value $out -Encoding utf8 }"
        },
        {
            "label": "fix-flame-agent-tools",
            "type": "shell",
            "command": "$p='.github/agents/flame-repo-parser.agent.md'; $t=Get-Content -Raw -Path $p; $t=$t.Replace('tools: [read, search, execute, web]','tools: [read, search, execute, web, agent]'); Set-Content -Path $p -Value $t -Encoding utf8"
        },
        {
            "label": "clean-outputs",
            "type": "shell",
            "command": "if (Test-Path out) { Remove-Item -Recurse -Force out }; New-Item -ItemType Directory -Path out\\runs -Force | Out-Null"
        },
        {
            "label": "run-new-workflow",
            "type": "shell",
            "command": "python",
            "args": [
                "scripts/run_parse_bundle.py",
                "--source",
                "${input:bundleSource}",
                "--type",
                "${input:bundleType}",
                "--repo",
                "${input:bundleRepo}",
                "--qdf-dir",
                "${input:bundleQdfDir}"
            ]
        },
        {
            "label": "run-hsd-workflow",
            "type": "shell",
            "command": "python",
            "args": [
                "scripts/run_parse_bundle.py",
                "--source",
                "${input:bundleSource}",
                "--type",
                "hsd",
                "--repo",
                "${input:bundleRepo}",
                "--hsd",
                "${input:bundleHsd}",
                "--output-root",
                "out/hsd"
            ]
        }
    ],
    "inputs": [
        {
            "description": "Source identifier for --source (FFR or repo path)",
            "id": "bundleSource",
            "type": "promptString"
        },
        {
            "description": "Workflow type for --type",
            "default": "both",
            "id": "bundleType",
            "type": "pickString",
            "options": [
                "both",
                "lineitem",
                "qdf",
                "hsd"
            ]
        },
        {
            "description": "Repo path for --repo (usually same as source)",
            "id": "bundleRepo",
            "type": "promptString"
        },
        {
            "description": "QDF folder path for --qdf-dir",
            "id": "bundleQdfDir",
            "type": "promptString"
        },
        {
            "description": "HSD id for --hsd (11 digits)",
            "id": "bundleHsd",
            "type": "promptString"
        }
    ]
}